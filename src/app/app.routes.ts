import { Routes } from '@angular/router';

// Single-page portfolio: all sections live on one scrollable route.
// Routes are kept in place (empty) so additional routes (e.g. a
// dedicated /project/aliz-tours case-study page) can be added later
// without restructuring the app.
export const routes: Routes = [];
